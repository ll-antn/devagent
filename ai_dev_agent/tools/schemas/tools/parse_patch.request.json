{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "Path to the unified diff patch file (relative to repo root or absolute)"
    },
    "include_context": {
      "type": "boolean",
      "description": "Include unchanged context lines in the output (default: false)",
      "default": false
    },
    "filter_pattern": {
      "type": "string",
      "description": "Optional regex pattern to filter files by path (e.g., 'stdlib/.*\\.ets$')"
    }
  },
  "required": ["path"]
}
