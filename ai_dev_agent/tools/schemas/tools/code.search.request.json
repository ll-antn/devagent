{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "code.search request",
  "type": "object",
  "required": ["query"],
  "properties": {
    "query": { 
      "type": "string",
      "description": "Text to search for. Uses fixed string matching by default - regex patterns will be treated as literal strings unless 'regex' is true."
    },
    "where": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional list of directories or files to limit the search scope"
    },
    "regex": { 
      "type": "boolean",
      "description": "Enable regex pattern matching. Default: false (uses fixed string matching)",
      "default": false
    },
    "max_results": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum number of matches to return",
      "default": 100
    }
  },
  "additionalProperties": false
}
