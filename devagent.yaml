# Essential CI commands
tests:
  cmd: "pytest"
lint:
  cmd: "ruff ."
types:
  cmd: "mypy ."
format:
  cmd: "ruff format ."

# Optional coverage thresholds
coverage:
  threshold:
    diff: 0.80
    project: 0.60

# Simplified iteration cap
react:
  iteration:
    global_cap: 500
